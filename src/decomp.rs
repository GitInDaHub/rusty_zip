use crate::h_tree::*;


/* This method takes .rzip file and decompresses its contents with
 * a unzip call but first it unhashes its contents
 */
pub fn runzip(in_file: &mut str){
    //XXX deals with uncompressing the files' contents
}
